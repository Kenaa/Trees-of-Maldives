## What this changes

<!-- One or two lines. If it adds tree records, say which. -->

## Checklist

- [ ] `python3 tools/validate.py` passes
- [ ] New records are `"verified": false` unless I have a source or have seen the tree
- [ ] Any new photo has `alt` text describing what is in the frame
- [ ] Any new interface string exists in **both** the `en` and `dv` blocks of `assets/js/i18n.js`
- [ ] Layout uses logical properties (`padding-inline`, `inset-inline-start`), not `left`/`right`
